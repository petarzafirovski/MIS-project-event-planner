import React from "react";
import { View, Text } from "react-native";

const SignOut = ({navigation, title}) => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Sign Out Placeholder</Text>
    </View>
  );
};

export default SignOut;
